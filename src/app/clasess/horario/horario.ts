export class Horario {

    public dia: string;
    public horas: string;

    constructor() {
        // data && this.hydrate(data);
    }

    // hydrate(data) {
    //     data.dia && (this.dia = data.dia);
    //     data.horas && (this.horas = data.horas);
    // }
}
