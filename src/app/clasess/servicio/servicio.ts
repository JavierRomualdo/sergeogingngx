export class Servicio {

    public src: string;
    public alt: string;
    public descripcion: string;

    constructor() {
        // data && this.hydrate(data);
    }

    // hydrate(data) {
    //     data.src && (this.src = data.src);
    //     data.alt && (this.alt = data.alt);
    //     data.descripcion && (this.descripcion = data.descripcion);
    // }
}
